{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(mvn test)",
      "Bash(mvn * test)",
      "Bash(mvn test *)",
      "Bash(mvn * test *)"
    ]
  }
}
