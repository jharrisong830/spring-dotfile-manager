spring:
  application:
    name: spring-dotfile-manager
  shell:
    interactive:
      enabled: false
    command:
      history:
        enabled: false
      clear:
        enabled: false
      script:
        enabled: false

spring-dotfile-manager:
  config:
    path: "{HOME}/.config/spring-dotfile-manager/config.yaml"
    default-repo-path: "{HOME}/dotfiles"
